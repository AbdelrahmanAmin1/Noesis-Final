{{SYSTEM_BASE}}

Task: Write a structured AI tutor video script on "{{CONCEPT}}" for a beginner learner.

Grounding status: {{GROUNDING_STATUS}}
{{GROUNDING_INSTRUCTION}}

Required slide sequence, 8-10 slides:
1. title
2. objectives
3. concept
4. analogy
5. diagram or source-backed visual only when useful
6. example
7. step_by_step
8. mistakes
9. recap
10. quiz

Each video must teach the concept, not merely label it. Include a clear definition, why it matters, a simple analogy, a concrete example or case, a step-by-step explanation, a common mistake, a recap, and a mini quiz. Use diagrams only when educationally useful; otherwise use comparison, flow, checklist, summary, or text-card visuals. Include code only when the source is actually about programming.

Source excerpts:
{{SOURCE_EXCERPTS}}

Material topic map:
{{TOPIC_MAP_BLOCK}}

{{EDUCATIONAL_CONTEXT_BLOCK}}

Output STRICT JSON only:
{
  "topic": "{{CONCEPT}}",
  "audienceLevel": "beginner",
  "learningObjectives": ["...", "...", "..."],
  "slides": [
    {
      "slideType": "title|objectives|concept|analogy|diagram|code|step_by_step|mistakes|recap|quiz",
      "title": "...",
      "bullets": ["...", "..."],
      "narration": "...",
      "visual": {
        "type": "mindmap|flow|comparison|code|summary|class_diagram|tree|stack_queue|linkedlist|bigo_chart",
        "description": "...",
        "nodes": ["...", "..."],
        "edges": [["...", "..."]]
      },
      "callouts": ["..."],
      "example_code": ""
    }
  ]
}

Rules:
- Produce 8-10 slides.
- If the topic map contains multiple topics, distribute slides across those topics in source order, include checkpoint/transition coverage, and do not focus only on the first topic.
- Use direct teaching narration. Do not start with "The uploaded material is about".
- Use concrete visual labels and state changes. For data structures, show operation state; for processes, show step flow; for tables or equations, preserve the source structure.
- Never use a page number as the center of a concept map.
- Every slide must have meaningful narration, 2-4 sentences, spoken like a tutor.
- Use 2-5 short bullets per slide. Keep each bullet under 70 characters.
- Use retrieved source chunks when available, but refer to them naturally as "the uploaded material" instead of showing raw chunk IDs.
- Do not claim unsupported details are from the uploaded material.
- Use uploaded source excerpts as course-specific truth. Use curated educational context only when it matches the uploaded subject.
- Do not expose raw internal JSON, source chunk IDs, debug trace, or internal metadata in learner-facing slide text.
- Never use vague placeholder output such as "Trace example", "Code sketch", "Apply main rule", "We will define", "Definition goes here", or "Example here".
- If grounding is LOW, the first slide narration and a callout must say the uploaded material did not contain enough specific information, then continue with general subject knowledge.
- Code slides should include compact Java or pseudocode only when relevant to programming material.
- Quiz slide should include one question and the answer in bullets or callouts.
